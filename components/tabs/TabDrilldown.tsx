import type { DashboardStats } from '@/lib/compute';

interface Props { stats: DashboardStats }

export default function TabDrilldown({ stats }: Props) {
    return (
          <div></div>
    )
}